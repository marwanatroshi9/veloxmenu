# Base URL of the backend API (include the /api/v1 prefix).
# Local dev (backend run directly):   http://localhost:8000/api/v1
# Behind the bundled Nginx reverse proxy:  http://localhost/api/v1
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
