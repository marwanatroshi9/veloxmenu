import type { Metadata, Viewport } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";

const inter = Inter({ subsets: ["latin"], variable: "--font-sans" });

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  viewportFit: "cover",
};

export const metadata: Metadata = {
  title: {
    default: "MenuHub — Digital Menus for Restaurants",
    template: "%s · MenuHub",
  },
  description:
    "Create, manage and share beautiful digital menus. Multi-restaurant SaaS platform with QR codes, analytics and a premium public menu experience.",
  openGraph: {
    title: "MenuHub",
    description: "Beautiful digital menus for modern restaurants.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
